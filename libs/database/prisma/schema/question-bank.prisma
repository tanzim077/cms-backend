model QuestionBank {
  id          Int     @id @default(autoincrement())
  title       String
  objectives  String
  description String
  is_active   Boolean @default(false)

  Question Question[]
  Quiz     Quiz?
}
