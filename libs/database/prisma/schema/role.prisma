model Role {
  id          Int                            @id @default(autoincrement())
  name        String                         @unique
  description String
  allowed     RolesOnAllowedPermissions[]
  restricted  RolesOnRestrictedPermissions[]
  createdAt   DateTime                       @default(now())
  updatedAt   DateTime                       @updatedAt
}
